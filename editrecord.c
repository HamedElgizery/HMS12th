#include<stdio.h>
edit_record(){
    FILE *fp = 0;
    int found= 0;
    headMessage("Enter patient name to edit record");
    fp= fopen("file.txt", "rb");
    if (fseek(fp, ))




    if (!found){
        printf("Record is not found.");
    }
    printf("Press any key to go to the main menu..");
    getchar();
    //



}